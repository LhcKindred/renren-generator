package ${package}.${moduleName}.service;

import com.baomidou.mybatisplus.extension.service.IService;
import ${package}.${moduleName}.entity.${className};
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;

import java.util.List;

/**
 * ${comments}Service
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Service extends IService<${className}> {

    void add(${className} ${classname});

    void updateInfo(${className} ${classname});

    void delete(${pk.attrType} ${pk.attrname});

    Page<${className}> listPage(${className} ${classname});

    ${className} info(${pk.attrType} ${pk.attrname});

##    void saveTmp(${className} ${classname});
}

